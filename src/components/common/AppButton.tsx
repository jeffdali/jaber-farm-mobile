import React from "react";
import { TouchableOpacity, Text } from "react-native";

const AppButton = () => {
  return (
    <TouchableOpacity>
      <Text>Button</Text>
    </TouchableOpacity>
  );
};

export default AppButton;
