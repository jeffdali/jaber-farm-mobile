import React from "react";
import { ActivityIndicator, View } from "react-native";

const AppLoader = () => {
  return (
    <View>
      <ActivityIndicator />
    </View>
  );
};

export default AppLoader;
